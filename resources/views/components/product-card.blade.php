<div class="">
    <a href="{{ route('products.show', $product) }}">
        <img class="h-fit max-w-full rounded-base" src="{{ asset('storage/' . $image) }}" alt="{{ $code }} image">
    </a>
</div>
